
@{
    ViewData["Title"] = "Unoccupied DVDs";
    Layout = "_AdminLayout";
}

@*<h1 class="text-center mb-3">DVDs Not Loaned</h1>
<h4 class="mt-1 mb-3">Displaying a list of all DVD titles in the shop where no copy of the title has been loaned in the last 31 days</h4>

<table class="table">
    <thead>
        <tr>
            <th>
                Copy Number
            </th>
            <th>
                DVD Title
            </th>
            <th>
                Release Date
            </th>
            <th>
                Loaned On
            </th>
            <th>
                Days Since
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
        <td>@item.CopyNumber</td>
        <td>@item.Title</td>
        <td>@item.ReleaseDate</td>
        <td>@item.LastLoan.DateOut.ToString("dd MMM yyyy")</td>
        
                <td>
                    @(DateTime.Now.Subtract(item.LastLoan.DateOut).Days)
                </td> 
        </tr>
}
    </tbody>
</table>*@

<div class="container-fluid py-4">
            <div class="row">
              <div class="col-12">
                <div class="card mb-6">
                  <div class="card-header pb-0">
                    <h2 class="mt-3 text-center text-warning">Unoccupied DVDs</h2>
                    <h6 class="text-xxs text-secondary mb-4 text-center">A list of DVDs which haven't been loaned in the last 31 days</h6>
                  </div>
                  <div class="card-body px-0 pt-0 pb-2">
                    <div class="table-responsive p-0">
                      <table class="table align-items-center mb-0">
                        <thead>
                          <tr>
                            <th class="text-uppercase text-secondary text-xs font-weight-bolder opacity-7 px-5">DVD Title</th>
                            <th class="text-uppercase text-secondary text-xs font-weight-bolder opacity-7">Copy Number</th>
                            <th class="text-uppercase text-secondary text-xs font-weight-bolder opacity-7">Release Date</th>
                            <th class="text-uppercase text-secondary text-xs font-weight-bolder opacity-7">Category</th>
                            <th class="text-center text-uppercase text-secondary text-xs font-weight-bolder opacity-7">Last Loaned On</th>
                            <th class="text-center text-uppercase text-secondary text-xs font-weight-bolder opacity-7">Days Since Last Loan</th>
                          </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model) {
                              <tr>
                                <td>
                                  <div class="d-flex px-2 py-1">
                                    <div>
                                      <img src=@item.Picture class="avatar avatar-sm me-3" alt="user1">
                                    </div>
                                    <div class="d-flex flex-column justify-content-center">
                                      <h6 class="mb-0 text-sm">@item.Title</h6>
                                    </div>
                                  </div>
                                </td>
                                <td class="align-middle">
                                  <p class="text-sm font-weight-bold mb-0 ms-5">@item.CopyNumber</p>
                                </td>
                                <td class="align-middle">
                                  <p class="text-sm font-weight-bold mb-0 ms-5">@item.ReleaseDate</p>
                                </td>
                                <td class="align-middle">
                                  <p class="text-sm font-weight-bold mb-0 ms-4">@item.Category</p>
                                </td>
                                <td class="align-middle text-start">
                                  <p class="text-sm font-weight-bold mb-0 ms-7">
                                      @item.LastLoan.DateOut.ToString("dd MMM yyyy")
                                  </p>
                                </td>
                                <td>
                                  <p class="text-success text-md mb-0 ms-8">
                                      @(DateTime.Now.Subtract(item.LastLoan.DateOut).Days) day(s)
                                  </p>
                                </td>
                              </tr>
                            }
                        </tbody>
                      </table>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>
