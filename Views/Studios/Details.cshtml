@model Studio

@{
    ViewData["Title"] = "Studio Details";
}

<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Details for @Model.StudioName</h1>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group">
                        <label asp-for="StudioName" class="control-label"></label>
                        <input asp-for="StudioName" class="form-control" readonly/>
                        <span asp-validation-for="StudioName" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <a class="btn btn-outline-success float-right" asp-action="Edit" asp-route-id="@Model.StudioNumber">Edit</a>
                        <a class="btn btn-outline-secondary" asp-action="Index"> Show All </a>
                    </div>
            </div>
        </div>
    </div>
</div>
